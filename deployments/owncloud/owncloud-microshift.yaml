"# This file has been split into multiple manifests for clarity.
# Use the bundled kustomization.yaml to apply the resources:
#
#   kubectl apply -k .
#
# Files created:
# - namespace.yaml
# - mariadb-pvc.yaml
# - owncloud-pvc.yaml
# - mariadb-deployment.yaml
# - mariadb-service.yaml
# - owncloud-deployment.yaml
# - owncloud-service.yaml
# - kustomization.yaml
"
